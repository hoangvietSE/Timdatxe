<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentStart="true"
    android:padding="@dimen/element_padding_small"
    android:paddingStart="@dimen/element_padding_normal">

    <ImageView
        android:id="@+id/imv_icon"
        android:layout_width="@dimen/margin_16_dp"
        android:layout_height="@dimen/margin_16_dp"
        android:layout_centerVertical="true"
        tools:src="@drawable/ic_starting_point_two" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/margin_8_dp"
        android:layout_toRightOf="@id/imv_icon"
        tools:text="@string/driver_request_detail_starting" />

    <TextView
        android:id="@+id/tv_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/tv_title"
        android:gravity="right"
        android:maxLines="2"
        android:paddingStart="@dimen/content_padding_normal"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_16"
        android:textStyle="bold"
        tools:text="Hội An, Quảng Nam, Việt Nam, Viet Nam" />

    <View
        android:id="@+id/line_view"
        style="@style/LineView"
        android:layout_below="@id/tv_detail"
        android:layout_marginTop="@dimen/margin_16_dp" />
</RelativeLayout>